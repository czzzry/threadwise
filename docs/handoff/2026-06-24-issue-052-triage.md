# Handoff

Current as of: 2026-06-24

## Read first

1. `AGENTS.md`
2. `CONTEXT.md`
3. `docs/v2-alignment.md`
4. `docs/prd.md`
5. `docs/checkpoints/current-operating-model-2026-06-22.md`
6. `docs/issues/052-prove-supervised-gmail-daily-use-on-new-mail.md`

## What changed

- Triaged `docs/issues/052-prove-supervised-gmail-daily-use-on-new-mail.md` into a `ready-for-agent` AFK slice.
- Tightened the slice around fresh Gmail mail only.
- Defined the proving window as the first `10` fresh Gmail daily runs that fetch at least one new message after frontier exhaustion.
- Defined the required per-run evidence and the rule that only recurring fresh-mail misses seen in at least `2` counted runs should trigger follow-on classifier work.

## Important nuance discovered during triage

- `python3 scripts/replay_gmail_readiness.py --account-id founder-test` currently reports overall `PASS` across stored Gmail batches, including `founder-test-batch-46`.
- `python3 scripts/check_gmail_readiness.py --account-id founder-test` currently reports `PAUSE` when it selects the latest daily report on disk.
- This mismatch appears because the latest stored daily report can still reflect a historical backfill run artifact rather than the fresh-mail proving window the next slice cares about.
- Because of that, issue `052` now requires `scripts/check_gmail_readiness.py --batch-id <fresh-batch-id>` for each counted fresh run. Do not treat "latest report on disk" as the proof target for this slice.

## Exact next step

1. Wait for the next genuinely fresh Gmail mail to arrive.
2. Run:
   - `python3 scripts/daily_live_gmail_run.py --account-id founder-test --batch-size 100`
3. If the command prints `No new messages found.`, record a no-op day and do not count it toward the proving window.
4. If it fetches a fresh batch, run:
   - `python3 scripts/check_gmail_readiness.py --account-id founder-test --batch-id <fresh-batch-id>`
5. Continue until `10` counted fresh runs exist, then write a short summary of pass/warn/pause totals and any recurring leftovers.
